{
  "name": "data-structure-validator",
  "version": "0.0.1",
  "description": "A validator for nested data structures",
  "main": "dist/resolver.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/lukepur/data-structure-validator"
  },
  "scripts": {
    "build": "node_modules/.bin/babel src --presets babel-preset-es2015 --out-dir dist",
    "test": "mocha --recursive",
    "test:watch": "mocha --recursive --watch",
    "prepublish": "npm run build"
  },
  "keywords": [
    "data",
    "validation",
    "structure"
  ],
  "author": "Luke Purton",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
